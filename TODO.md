- CLAUDE.md claims new tmpfiles rules need a reboot. The VM now carries `systemd-tmpfiles-resetup.service` (`ExecStart=systemd-tmpfiles --create --remove`, restarted by an X-Restart-Triggers file that tracks `tmpfiles.d`), which is upstream's switch-time twin of the boot-only setup unit — so the gotcha may be stale. Confirm at the next `switch` that a brand-new rule lands without a reboot, then rewrite or delete the section.
- Commit 6ea55d0 message carries a leftover "# Conflicts:" template block; scrub with a history rewrite (e.g. `git rebase -r 6ea55d0^` reword) before any publication, then force-sync the VM clone
